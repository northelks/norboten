# The Welcome Message Nobody Can Read

Users say the welcome message in `/srv/example/motd` shows "Permission denied" for everyone except
root. It should be readable by every user on the system, and stay that way after a reboot.

<!-- Symptoms only. Never name the cause, the command, or the fix. See docs/lab-spec.md §6. -->
