server {
    listen 8080;
    server_name _;

    location / {
        proxy_pass http://127.0.0.1:11435;
        proxy_http_version 1.1;
        proxy_buffering on;
        proxy_read_timeout 5s;
    }
}
