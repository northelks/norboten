---
# Collections the playbooks use. deploy/install-server.py installs these on the server itself;
# on a laptop: ansible-galaxy collection install -r ansible/requirements.yml
collections:
  - name: ansible.posix
    version: ">=2.0.0"
  - name: community.general
    version: ">=10.0.0"
