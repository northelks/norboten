# The laptop stack: the site on :8080, plain HTTP, no certificates.
:8080 {
	encode zstd gzip
	root * /srv/site
	# the same collapse as production, so an address here is the address there
	@index path_regexp idx ^(.*)/index\.html$
	redir @index {re.idx.1}/ permanent
	file_server
}
